import { StyleSheet } from 'react-native'

const sttyles = StyleSheet.create({
    content: {
        flexGrow: 0.5,
        width: '100%',
        alignItems: 'center',
        justifyContent: 'space-evenly'
    },
})