import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    content: {
        flexGrow: 0.5,
        width: '100%',
        alignItems: 'center',
        justifyContent: 'space-evenly'
    },

    text: {
        color: '#fff',
        fontSize: 30
    },
    botao: {
        height: 50,
        width: 200,
        color: '#000'
    }
})