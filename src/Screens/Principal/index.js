import React from 'react'
import { View, Text, ScrollView } from 'react-native'


export default function Pincipal({ navigation }) {
    return (
        <>
            <ScrollView>
                <Text>Só pra ver se foi.</Text>
            </ScrollView>
        </>
    )
}